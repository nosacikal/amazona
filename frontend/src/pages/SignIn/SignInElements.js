import styled from 'styled-components'

export const SignInForm = styled.form`
  margin: 0 auto;
  max-width: 55rem;
  padding: 1rem;

  & > div {
    display: flex;
    flex-direction: column;
  }
`
