import styled from 'styled-components'

export const ProductRating = styled.div`
  span {
    color: #f0c040;
    margin: 0.1rem;
  }

  span:last-child {
    color: #000;
    margin-left: 0.4rem;
  }
`
